<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.kpmadan.health_ots">

    <meta-data
        android:name="com.google.android.gms.version"
        android:value="@integer/google_play_services_version"
        tools:replace="android:value" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FirebaseOfflineData"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignInActivity" />
        <activity
            android:name=".HomeOtsScreen"
            android:label="@string/title_activity_home_ots_screen" />
        <activity
            android:name=".FindBookActivity"
            android:label="@string/title_activity_find_book"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ListBooking"
            android:label="@string/title_activity_list_booking"
            android:theme="@style/AppTheme" />
        <activity android:name=".EmailRegisterActivity" />
        <activity android:name=".LoginWithEmailActivity" />
        <activity android:name=".UserLoginInfoActivity" />
        <activity android:name=".SignupOptionActivity" />
        <activity android:name=".SIgnInWithPhoneActivity" />
        <activity android:name=".SignInPhoneData" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>